version: "3.9"

services:
  json_server:
    image: php:8.1
    container_name: json_server
    command: php -S 0.0.0.0:3003
    working_dir: /app
    ports:
      - 3003:3003
    volumes:
      - .:/app
